# EnRollify

Educational landing page built from `design.json` — playful modern hero with purple-mint palette, pill controls, and hard-offset shadows.

## Run locally

```bash
npm install
npm run dev
```

Open the URL shown in the terminal (typically `http://localhost:5173`).

## Design source

All visual tokens (colors, typography, spacing, components) are mapped from `design.json`.
